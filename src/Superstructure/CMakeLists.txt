
target_include_directories(esmf PRIVATE ${ESMF_SOURCE_DIR}/src/Superstructure/../include)

if(${Git_FOUND})
    string(REGEX REPLACE "^([0-9]+)\.([0-9]+)(\.[0-9]+)?$" "\\1.\\2" ESMF_VERSION_STRING_GIT ${GIT_VERSION_STRING})
    add_compile_definitions(ESMF_VERSION_STRING_GIT="${ESMF_VERSION_STRING_GIT}")
endif()
add_subdirectory(State)
add_subdirectory(WebServices)
add_subdirectory(IOAPI)
add_subdirectory(InternalState)
add_subdirectory(TraceAPI)
add_subdirectory(StateReconcile)
add_subdirectory(ESMFMod)
add_subdirectory(NamedAlias)
add_subdirectory(AppDriver)
add_subdirectory(InfoAPI)
if(ESMF_MAPPER_BUILD)
  add_subdirectory(Mapper)
endif()
add_subdirectory(AttachMethods)
add_subdirectory(PreESMFMod)
add_subdirectory(Component)
add_subdirectory(AttributeAPI)
